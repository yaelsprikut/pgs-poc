import { render, screen } from '@testing-library/react';
// import App from './App';

// test('renders Make sure this text is here text', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Make sure this text is here/i);
//   expect(linkElement).toBeInTheDocument();
// });

test('expect app test to run', () => {
  expect(true).toBeTruthy();
});